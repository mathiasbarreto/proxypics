<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url();">
  <div class="container">
    <h1><strong>The evolution </strong> of real state <strong>inspections and appraisals</strong></h1>
    <p> Up to date photos and property details in a matter of hours</p>
    <% unless requester_signed_in? %>
       <%= link_to 'Login to Order now', new_requester_session_path, class: "btn btn-flat" %> 
    <% end %>
     <% if requester_signed_in? %>
      <%= link_to 'Order now', orders_path, class: "btn btn-flat" %> 
    <% end %> ||
    <% unless assignee_signed_in? %>
      <%= link_to 'Login to Make mokey', new_assignee_session_path, class: "btn btn-flat" %> 
      
    <% end %>
    <% if assignee_signed_in? %>
      <%= link_to 'Submit photos', orders_path, class: "btn btn-flat" %> 
    <% end %>  
  </div>
</div>
